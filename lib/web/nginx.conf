events {
    worker_connections 1024;
}

http {
    server {
        listen 3000;

        server_name _;

        location / { 
            root /web/bento_web/dist;
            try_files $uri $uri/ /index.html;
        }
    }
}